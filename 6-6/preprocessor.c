#include "hashtable.h"
#include <stdio.h>
#define getch() getc(stdin)
#define ungetch(c) ungetc(c, stdin)

int main(int argc, char* argv[])
{
    int c;
    while((c = getch()) != EOF) {
        
    }
    return 0;
}
